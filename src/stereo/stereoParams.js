/** Anaglyph & parallax-barrier stereo — Tweakpane-bound and serialized in scene JSON. */
export const stereoParams = {
  anaglyphEnabled: false,
  parallaxBarrierEnabled: false,
  eyeSep: 0.064,
  planeDistance: 200,
};

export const STEREO_PARAM_KEYS = [
  "anaglyphEnabled",
  "parallaxBarrierEnabled",
  "eyeSep",
  "planeDistance",
];
